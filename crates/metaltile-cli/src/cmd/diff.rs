//! `tile diff` command — compare benchmark results against a saved baseline.
//!
//! Stub — full implementation deferred.

use crate::CliError;

/// Run the diff subcommand.
pub fn run(_args: &crate::DiffArgs) -> Result<(), CliError> {
    eprintln!("diff: not yet implemented");
    Ok(())
}
