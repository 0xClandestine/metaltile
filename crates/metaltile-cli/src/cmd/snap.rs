//! `tile snap` command — save benchmark results as a regression baseline.
//!
//! Stub — full implementation deferred.

use crate::CliError;

/// Run the snap subcommand.
pub fn run(_args: &crate::SnapArgs) -> Result<(), CliError> {
    eprintln!("snap: not yet implemented");
    Ok(())
}
